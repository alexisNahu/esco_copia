---
import { Image } from "astro:assets";
import logo from '../assets/logo-esco.png';
---

<nav class="relative z-50 md:bg-transparent bg-white md:border-b-0 border-b border-gray-100">
    <div class="marg mx-auto flex flex-row items-center justify-between px-6 py-4">
        <div class="flex flex-col items-center">
            <a href="/">
                <Image src={logo} alt="ESCO Estudio Contable" class="w-[100px] md:w-[250px]" />
            </a>
        </div>

        <button
                id="menu-toggle"
                class="flex flex-col gap-1.5 z-50 md:hidden p-3 -mr-2 focus:outline-none"
                aria-label="Abrir menú"
        >
            <div class="w-6 h-0.5 bg-lila-esco transition-all duration-300 line1"></div>
            <div class="w-6 h-0.5 bg-lila-esco transition-all duration-300 line2"></div>
            <div class="w-6 h-0.5 bg-lila-esco transition-all duration-300 line3"></div>
        </button>

        <ul
                id="menu-items"
                class="hidden md:flex opacity-0 scale-95 md:scale-100 md:opacity-100 absolute md:static top-full left-0 w-full md:w-auto bg-white md:bg-transparent md:shadow-none shadow-lg flex-col md:flex-row items-center gap-0 md:gap-4 text-sm md:text-[20px] uppercase font-semibold py-4 md:py-0 transition-all duration-300 z-40"
        >
            <li class="w-full text-center md:w-auto border-b md:border-b-0 border-gray-100">
                <a href="#nosotros" class="mobile-link hover:text-lila-esco transition-colors block py-5 md:py-0 md:pb-[12px]">Nosotros</a>
            </li>
            <li class="w-full text-center md:w-auto border-b md:border-b-0 border-gray-100 md:border-l md:border-lila-esco md:pl-4">
                <a href="#servicios" class="mobile-link hover:text-lila-esco transition-colors block py-5 md:py-0 md:pb-[12px]">Servicios</a>
            </li>
            <li class="w-full text-center md:w-auto border-b md:border-b-0 border-gray-100 md:border-l md:border-lila-esco md:pl-4">
                <a href="#equipo" class="mobile-link hover:text-lila-esco transition-colors block py-5 md:py-0 md:pb-[12px]">Equipo</a>
            </li>
            <li class="w-full text-center md:w-auto md:border-l md:border-lila-esco md:pl-4">
                <a href="#contacto" class="mobile-link hover:text-lila-esco transition-colors block py-5 md:py-0 md:pb-[12px]">Contactos</a>
            </li>
        </ul>
    </div>
</nav>

<script>
    const menuToggle = document.getElementById('menu-toggle');
    const menuItems = document.getElementById('menu-items');
    const mobileLinks = document.querySelectorAll('.mobile-link');

    function toggleMenu() {
        if (menuItems.classList.contains('hidden')) {
            menuItems.classList.remove('hidden');
            setTimeout(() => {
                menuItems.classList.remove('opacity-0', 'scale-95');
                menuItems.classList.add('flex');
            }, 10);
        } else {
            menuItems.classList.add('opacity-0', 'scale-95');
            menuItems.addEventListener('transitionend', function handler() {
                menuItems.classList.add('hidden');
                menuItems.classList.remove('flex');
                menuItems.removeEventListener('transitionend', handler);
            });
        }

        // Animación de las líneas para formar la "X"
        menuToggle.querySelector('.line1').classList.toggle('rotate-45');
        menuToggle.querySelector('.line1').classList.toggle('translate-y-2');
        menuToggle.querySelector('.line2').classList.toggle('opacity-0');
        menuToggle.querySelector('.line3').classList.toggle('-rotate-45');
        menuToggle.querySelector('.line3').classList.toggle('-translate-y-2');
    }

    menuToggle.addEventListener('click', toggleMenu);

    mobileLinks.forEach(link => {
        link.addEventListener('click', () => {
            if (window.innerWidth < 768) {
                toggleMenu();
            }
        });
    });
</script>

<style>
    .rotate-45 {
        transform: rotate(45deg);
    }
    .-rotate-45 {
        transform: rotate(-45deg);
    }
    .translate-y-2 {
        transform: translateY(8px);
    }
    .-translate-y-2 {
        transform: translateY(-8px);
    }
</style>
